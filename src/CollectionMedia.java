import lombok.NonNull;

public class CollectionMedia implements Collection{

    @Override
    public byte[] Add(@NonNull byte[] data) {
        return new byte[0];
    }

    @Override
    public byte[] Remove(byte[] data) {
        return new byte[0];
    }
}
